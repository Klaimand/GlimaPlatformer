using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TriggerHighScore : MonoBehaviour
{
    public float Temps = 2f;
    private float TempsReel;
    private bool ObjActif;

    void Start()
    { 
   
    }  

    void Update()
   {
   //     if (totalTime > PlayerPrefs.GetInt("HighScore", 0))
  //      {
  //          GetComponent(Image).enable = false;
  //          GetComponent(Image).enable = true;
      
    } 
}

